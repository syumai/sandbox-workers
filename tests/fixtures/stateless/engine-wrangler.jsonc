{
  "name": "sandbox-stateless-fixture-engine",
  "main": "engine-entry.ts",
  "compatibility_date": "2026-09-04",
  "workers_dev": false,
  "preview_urls": false,
  // No durable_objects/migrations: this runtime Worker has no INTERPRETER
  // binding, so it serves only /execute; GET /interpreter reports
  // { contexts: false } and every /interpreters/* route answers 400 (see
  // docs/sandbox-1-0-design.md, "Ruby" / stateless deployments).
}
